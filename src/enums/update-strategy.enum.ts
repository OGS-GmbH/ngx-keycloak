
/**
 * Enumeration for different token update strategies
 *
 * @since 1.0.0
 * @author Simon Kovtyk
 */
export enum UpdateStrategy {
  /**
   *
   *
   */
  BOTH = "both",
  INTERVAL = "interval",
  INTERCEPTOR = "interceptor"
}
